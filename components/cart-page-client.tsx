"use client";import {useEffect,useState} from "react";import {money} from "@/lib/catalog";
type Item={id:string;quantity:number;unitPrice:number;product:{name:string;brand:string;volume:string;image:string}};
export function CartPageClient(){const[items,setItems]=useState<Item[]>([]);const[loading,setLoading]=useState(true);async function load(){const r=await fetch("/api/cart");setItems((await r.json()).items);setLoading(false)}useEffect(()=>{load()},[]);async function remove(id:string){await fetch(`/api/cart?item=${id}`,{method:"DELETE"});load()}const total=items.reduce((s,i)=>s+i.unitPrice*i.quantity,0);if(loading)return <p>Carregando seu carrinho…</p>;if(!items.length)return <div className="empty-cart"><h2>Seu carrinho está vazio.</h2><a href="/perfumes">Descobrir perfumes</a></div>;return <div className="cart-layout"><section>{items.map(i=><article className="cart-item" key={i.id}><img src={i.product.image} alt={i.product.name}/><div><p>{i.product.brand}</p><h2>{i.product.name}</h2><small>{i.product.volume} · Quantidade {i.quantity}</small><button className="remove-item" onClick={()=>remove(i.id)}>Remover</button></div><strong>{money(i.unitPrice*i.quantity)}</strong></article>)}</section><aside><h2>Resumo do pedido</h2><p><span>Subtotal</span><b>{money(total)}</b></p><p><span>Frete</span><em>Calculado no checkout</em></p><div className="cart-total"><span>Total</span><b>{money(total)}</b></div><a href="/checkout">Ir para o checkout</a><small>Preço e estoque serão validados no servidor</small></aside></div>}

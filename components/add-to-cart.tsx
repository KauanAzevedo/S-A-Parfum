"use client";import {ShoppingBag,Zap} from "lucide-react";import {useState} from "react";
export function AddToCart({productName,productId}:{productName:string;productId?:string}){const[added,setAdded]=useState(false);const[error,setError]=useState("");async function add(){setError("");const id=productId??productName.normalize("NFD").replace(/[\u0300-\u036f]/g,"").toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/(^-|-$)/g,"");const r=await fetch("/api/cart",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({productId:id,quantity:1})});if(r.ok)setAdded(true);else setError((await r.json()).error)}return <div className="buy-actions"><button onClick={add} className="buy-main"><ShoppingBag size={18}/>{added?"Adicionado ao carrinho":"Adicionar ao carrinho"}</button><a href="/carrinho"><Zap size={18}/> Comprar agora</a>{added&&<p role="status">{productName} foi adicionado. <a href="/carrinho">Ver carrinho</a></p>}{error&&<p role="alert">{error}</p>}</div>}

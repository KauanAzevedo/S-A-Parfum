import { Heart, ShoppingBag } from "lucide-react";
import { money, type Product } from "@/lib/catalog";
export function ProductCard({product}:{product:Product}){return <article className="product-card"><a href={`/produto/${product.slug}`} className="product-image">{product.badge&&<span>{product.badge}</span>}<button aria-label={`Favoritar ${product.name}`}><Heart size={18}/></button><img src={product.image} alt={`Perfume ${product.name}`} loading="lazy"/></a><div className="product-info"><p>{product.brand}</p><a href={`/produto/${product.slug}`}><h3>{product.name}</h3></a><small>{product.volume} · {product.gender}</small><div className="price">{product.oldPrice&&<del>{money(product.oldPrice)}</del>}<b>{money(product.price)}</b><em>ou 6x de {money(product.price/6)}</em></div><a className="add" href={`/produto/${product.slug}`}><ShoppingBag size={17}/> Ver perfume</a></div></article>}
